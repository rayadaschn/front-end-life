import{_ as a,X as e,Y as s,$ as n}from"./framework-5dd7fabc.js";const c={},i=n,t=e,o=s,d=i(`<p>不知道你有没有注意到，在 JavaScript 中，有些方法名是全小写的，而有些方法名则是驼峰式的。</p><p>例如 <code>instanceof</code> 里的 of 是小写，而 <code>indexOf</code> 里的 Of 是大写。</p><blockquote><p>看起来像是拼写不统一，其实背后是<strong>两套完全不同的命名世界</strong>。</p></blockquote><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code>obj <span class="token keyword">instanceof</span> <span class="token class-name">Array</span>
<span class="token string">&#39;hello&#39;</span><span class="token punctuation">.</span><span class="token function">indexOf</span><span class="token punctuation">(</span><span class="token string">&#39;e&#39;</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="原因" tabindex="-1"><a class="header-anchor" href="#原因" aria-hidden="true">#</a> 原因</h2><p><code>instanceof</code> 是语法，不是函数。</p><p><code>instanceof</code> 并不是一个普通方法名，它是语言内置的<strong>关键字（操作符）</strong>。类似的还有 <code>typeof</code>、<code>delete</code>、<code>in</code> 等等。这些都是 JavaScript 语法的一部分，必须遵循<strong>全小写</strong>的命名规则。</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code>obj <span class="token keyword">instanceof</span> <span class="token class-name">Array</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="关键字的命名规则" tabindex="-1"><a class="header-anchor" href="#关键字的命名规则" aria-hidden="true">#</a> 关键字的命名规则</h3><p>几乎所有主流语言都有一个共识：</p><blockquote><p><strong>关键字一律使用小写。</strong></p></blockquote><p>原因很简单：</p><ul><li>它们属于“语言本身”</li><li>不应该与变量、函数、类混淆</li><li>小写更接近自然语言中的“语法词”</li></ul><p>所以：</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">instanceof</span>   <span class="token comment">// 合法</span>
instanceOf   <span class="token comment">// ❌ 语法错误</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>在这里，<code>of</code> 只是关键字的一部分，而不是一个可以单独识别的单词。</p><h3 id="标准库方法名规则" tabindex="-1"><a class="header-anchor" href="#标准库方法名规则" aria-hidden="true">#</a> 标准库方法名规则</h3><p>API 的命名遵循的是<strong>驼峰命名法（camelCase）</strong>：</p><ul><li>第一个单词小写</li><li>后续单词首字母大写</li></ul><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token string">&#39;hello&#39;</span><span class="token punctuation">.</span><span class="token function">indexOf</span><span class="token punctuation">(</span><span class="token string">&#39;e&#39;</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这里的 <code>Of</code> 是一个“独立语义单词”，自然要大写。同类例子还有：</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code>lastIndexOf
startsWith
endsWith
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,22),l=[d];function r(p,u){return t(),o("div",null,l)}const m=a(c,[["render",r],["__file","JS36.html.vue"]]);export{m as default};
